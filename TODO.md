# TODO

> Only user-actionable items (things Claude can't do). Project/engineering work goes in IMPROVEMENTS.md.

This file is exclusively for real-world actions **you** must take (things Claude cannot do) —
e.g. add a shell alias to `~/.zshrc`, bookmark a file, rotate a key, click through a console.

## Open
_Nothing pending._ The dashboard is feature-complete and the three entry points
(bookmarked HTML, `chats` alias, `/chats` command) are already installed.

## Done
- [x] ~~Add `chats` alias to `~/.zshrc` pointing at `~/claude/export_chat/chats_dashboard.py --open`~~
- [x] ~~Bookmark `~/.claude/history-dashboard/index.html` for zero-Claude viewing~~

---
Engineering work, design decisions, and the build history live in **IMPROVEMENTS.md** and in
project memory (`project_state.md`) — not here.
